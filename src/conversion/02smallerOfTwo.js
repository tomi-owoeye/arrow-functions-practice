/**
 * @param {number} a first number
 * @param {number} b second number
 * @returns {number}  */
function smallerOfTwo(a, b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}
export { smallerOfTwo };

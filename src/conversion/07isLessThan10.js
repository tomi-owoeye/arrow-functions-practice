/**Returns true if the given number is less than 10, else false.
 * @param {number} n: number to examine
 * @returns {boolean} true if n is less than 10
 */
function isLessThan10(n) {
    return n < 10;
}
export { isLessThan10 };

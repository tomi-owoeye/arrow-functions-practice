/** returns a two-element array with the two arguments as the elements.
 * @param {any} a first element to go in array
 * @param {any} b second element to go in array
 * @returns {any[]} array of two elements, [a, b]
 */
function createArray(a, b) {
    return [a, b];
}

export { createArray };
